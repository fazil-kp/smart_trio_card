name: example
description: A customizable trio card carousel widget for Flutter with smooth animations, looping transitions, and interactive front card switching.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.5.1
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  smart_trio_card:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true
